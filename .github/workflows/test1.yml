name: test-GH
on: workflow_dispatch
jobs:
  test-case:
   runs-on: ubuntu-latest
   steps:
     - name: test-step
       run: |
         echo "Hello world"
         echo "Enter second line for running"
         echo "Enter third line for running"
